<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= site_name %>: <%= yield :title %>
    </title>
    <link rel="shortcut icon" href="/assets/favicon.ico" />

    <meta name="viewport" content="initial-scale=1.0">
    <meta name="description" content="UFCfit">

    <%= csrf_meta_tag %>
    <%= yield :head %>

  </head>
  <body >

    <%= yield %>

    <%= yield :bottom %>

  </body>
  <%= render 'shared/google_analytics' unless @disable_ga %>

  <%= yield :below_body %>
</html>
